# ═══════════════════════════════════════════════
#  AjayaDesign Automation — Environment Variables
#  Copy to .env and fill in your values
# ═══════════════════════════════════════════════

# n8n credentials
N8N_USER=admin
N8N_PASSWORD=changeme

# GitHub personal access token (for gh CLI + GitHub Models AI)
# Create at: https://github.com/settings/tokens
# Scopes needed: repo, workflow, admin:org
# Also used for free AI generation via GitHub Models API
GH_TOKEN=

# AI model (via GitHub Models — free with your GH_TOKEN)
# Options: gpt-4o, gpt-4o-mini, Meta-Llama-3.1-405B-Instruct
AI_MODEL=gpt-4o

# Telegram bot notification
# Get bot token from @BotFather
# Get chat ID by messaging the bot, then:
#   curl https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
